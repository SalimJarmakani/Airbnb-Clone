<!DOCTYPE html>
<html>
<head>
  <title>{{listing.name}}</title>
</head>
<body>
  <div class="container">
    <div class="row">
      <div class="col-md-8">
        <h1>{{listing.name}}</h1>
        <img src="{{listing.images[0]}}" alt="Listing Image" class="img-fluid">
        <p>{{listing.description}}</p>
      </div>
      <div class="col-md-4">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title">Details</h5>
            <ul class="list-group list-group-flush">
              <li class="list-group-item"><strong>Neighborhood Overview:</strong> {{listing.neighborhood_overview}}</li>
              <li class="list-group-item"><strong>Property Type:</strong> {{listing.property_type}}</li>
              <li class="list-group-item"><strong>Room Type:</strong> {{listing.room_type}}</li>
              <li class="list-group-item"><strong>Accommodates:</strong> {{listing.accommodates}}</li>
              <li class="list-group-item"><strong>Price:</strong> ${{listing.price}}</li>
              <li class="list-group-item"><strong>Review Score:</strong> {{listing.review_score_value}}</li>
              <li class="list-group-item"><strong>Cancellation Policy:</strong> {{listing.cancellation_policy}}</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>